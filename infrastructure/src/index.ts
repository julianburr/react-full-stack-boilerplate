import * as apiStack from './api-stack';
import * as webStack from './web-stack';

export const apiProjectId = apiStack.id;
export const apiProjectName = apiStack.name;
export const apiDbUrl = apiStack.dbUrl;

export const webProjectId = webStack.id;
export const webProjectName = webStack.name;

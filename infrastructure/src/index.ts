import * as apiStack from './api-stack';
import * as webStack from './web-stack';

export const webProjectName = webStack.projectName;
export const webUrl = webStack.url;

export const apiProjectName = apiStack.projectName;
export const apiUrl = apiStack.url;
